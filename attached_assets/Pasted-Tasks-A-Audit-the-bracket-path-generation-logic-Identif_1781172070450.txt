Tasks:

A. Audit the bracket path generation logic:

Identify where group finish position, bracket slot, opponent options, and round progression are calculated.
Check whether team identity and bracket seed/slot are being mixed incorrectly.
Ensure C1/C2/F1/F2 labels are derived from the selected scenario, not hardcoded from default standings.

B. Audit probability calculation:

Find why impossible or low-sample scenarios become 100.0%.
Add guards so missing data, zero samples, or invalid paths never display as 100%.
Use null/insufficient-data states where appropriate.
Add a minimum sample threshold and meaningful confidence messaging.

C. Fix state/navigation issues:

Ensure clicking an opponent flag immediately updates route/state.
Avoid stale selectedTeam state.
Ensure route params, selected scenario, selected opponent, and displayed bracket path stay synchronized.
Add loading state only if needed, but do not delay or require another click.

D. Add tests:

Unit tests for bracket slot generation.
Unit tests for group winner/runner-up/third-place scenarios.
Tests for Argentina third-place vs Turkey path.
Tests for Brazil C2 vs Netherlands path.
Tests for Brazil vs Spain final constraints.
Tests ensuring impossible/zero-sample probabilities do not show 100%.
UI/component tests for opponent selector in Round of 16, Quarterfinal, Semifinal, and Final.
UI test for clicking opponent flag and immediately loading that opponent’s path.

E. Add debug logging only temporarily:

Log selected team, group position, bracket slot, selected opponent, round, generated path, sample count, and probability source.
Remove or gate logs after fixing.

Expected outcome:

Bracket cards should always show correct team finish positions.
Opponent selection should work in every round.
Opponent flag click should navigate immediately.
Invalid or low-sample scenarios should not show fake 100% probabilities.
Tests should prevent regressions across Argentina, Brazil, Spain, Netherlands, Turkey, France, and Croatia examples.